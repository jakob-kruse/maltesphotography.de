# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: NicePWD
    ports:
      - 5150:5432

  adminer:
    image: adminer
    restart: always
    ports:
      - 5151:8080
